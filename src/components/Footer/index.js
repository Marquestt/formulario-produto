import styles from './Footer.module.css';

const Footer = () => {
    return(
        <footer className={styles.footer}>
            <p>
                Desenvolvido por <strong>Thatyane Marques</strong>.
            </p>
        </footer>
    );
}

export default Footer;
